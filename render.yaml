# Render.com deployment configuration
# Visit https://render.com to set up your service

# Build Command:
pip install -r requirements.txt

# Start Command:
uvicorn app.main:app --host 0.0.0.0 --port $PORT
